# ============================================================
# 开发环境配置
# ============================================================

spring:
  # 数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/xiaozhi_5?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
    username: root
    password: rootroot

# 本地环境邮箱配置
email:
  smtp:
    username: xxx
    password: xxx

xiaozhi:
  server:
    # domain 格式为 IP:端口，端口必须与实际 server.port 一致
    domain: 192.168.1.100